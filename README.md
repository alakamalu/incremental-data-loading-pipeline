# incremental-data-loading-pipeline
Production-style incremental ETL pipeline using Python, Pandas, and SQLite to load only new e-commerce order data into a data warehouse based on timestamp change detection.
